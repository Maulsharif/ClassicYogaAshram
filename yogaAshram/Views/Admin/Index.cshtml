@inject UserManager<Employee> UserManager
@using Microsoft.AspNetCore.Identity
@model AdminIndexModel

@{
    ViewBag.Title = "Главная";
    Layout = "_Layout";
}
<h2>Админ @Model.Branch.Name</h2>

<div class="container" >

    <div class="row" >
        <div class="col-sm-4">
            <ul class="list-group ">


                <div id="info">
                    <h6 id="name">ФИО : @Model.Employee.NameSurname </h6>
                    <h6 id="email">Email : @Model.Employee.Email</h6>
                    <h6 id="login">Логин : @Model.Employee.UserName</h6>
                </div>
                <li class="list-group-item">
                    <a data-target="editProfileModal" id="@Url.Action("GetEditModalAjax", "Employees")" class="getModal dropdown-item cursor-p">
                        Редактировать профиль
                    </a>
                </li>
                <li class="list-group-item">
                    <a data-target="changePasswordModal" id="@Url.Action("GetChangePasswordModalAjax", "Employees")" class="getModal dropdown-item cursor-p">
                        Изменить пароль
                    </a>
                </li>
                <li class="list-group-item"><a asp-action="CreateClients" asp-controller="Clients" asp-route-branchId="@Model.Branch.Id" class="dropdown-item cursor-p">Добавить клиента</a></li>
                <li class="list-group-item">    <a asp-action="Scheduele" asp-controller="Admin" asp-route-branchId="@Model.Branch.Id" class="dropdown-item cursor-p" >Календарь</a></li>
                <li class="list-group-item">    <a asp-action="Trials" asp-controller="Clients" asp-route-branchId="@Model.Branch.Id" class="dropdown-item cursor-p">Пробники</a></li>
                <li class="list-group-item">    <a asp-action="" asp-controller="Clients" asp-route-branchId="@Model.Branch.Id" class="dropdown-item cursor-p">Клиенты</a></li>
            </ul>
        </div>
      

    </div>
</div>


@section Scripts{
    @{
        await Html.RenderPartialAsync("../Employees/PartialViews/GetModalScripts");
    }
}